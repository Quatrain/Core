import { CoreHero, CoreFeatureCard, CoreStatusBadge } from '../components/index.jsx'
import { Tabs } from 'nextra/components'

<CoreHero />

---

## 🌟 At a Glance

**Quatrain Core** is an enterprise-grade, modular TypeScript framework designed to build resilient, cloud-agnostic applications. Built around the **Adapter Pattern**, it completely decouples domain modeling and business rules from the underlying infrastructure.

Write your domain logic once, then seamlessly swap or mix:
* **Databases**: PostgreSQL, SQLite, Firestore, or REST backends.
* **Authentication**: Supabase Auth, Firebase Auth, OIDC / OAuth2, PocketBase, or HTTP Basic.
* **Storage**: AWS S3, MinIO, Local Filesystem, Google Cloud Storage, or Git.
* **Queuing & Messaging**: RabbitMQ (AMQP), MQTT, SQLite queues, AWS SQS, or GCP Pub/Sub.

Target **100% on-premise sovereign setups** or modern **Backend-as-a-Service (BaaS) cloud platforms** with the exact same codebase.

---

## 🎯 Core Principles

<div className="grid grid-cols-1 md:grid-cols-2 gap-5 my-6">
  <CoreFeatureCard
    icon="🏛️"
    layer="Principle 1"
    badge="Pure Domain"
    badgeLevel="optimal"
    title="Clean Domain Abstraction"
    description="Entities, validation rules, and business logic live in @quatrain/core with zero external dependencies. They remain fully testable in-memory."
    href="/okf/domain-modeling"
  />
  <CoreFeatureCard
    icon="🛡️"
    layer="Principle 2"
    badge="Sovereign"
    badgeLevel="brand"
    title="Zero Vendor Lock-In"
    description="Run your stack on self-hosted bare metal (PostgreSQL + RabbitMQ + S3 storage) or managed cloud platforms with a single configuration line."
    href="/okf/persistence-adapters"
  />
  <CoreFeatureCard
    icon="⚡"
    layer="Principle 3"
    badge="Zero Boilerplate"
    badgeLevel="nominal"
    title="Active Record & Repositories"
    description="Every entity provides dynamic, strongly-typed repositories with built-in schema validation, serialization, relationships, and aggregations."
    href="/okf/persistence-adapters"
  />
  <CoreFeatureCard
    icon="📦"
    layer="Principle 4"
    badge="71 Packages"
    badgeLevel="optimal"
    title="Fine-Grained Modularity"
    description="A factorized monorepo of 71 independent packages. Import only what your service needs to keep production bundles light and performant."
    href="/packages"
  />
</div>

---

## 🚀 Quick Start

### 1. Install Dependencies

Install the core modeling engine and the backend adapter of your choice:

<Tabs items={['Yarn 4', 'npm', 'Bun', 'Tycho Knowledge']}>
  <Tabs.Tab>
    ```bash
    yarn add @quatrain/core @quatrain/backend @quatrain/backend-postgres
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash
    npm install @quatrain/core @quatrain/backend @quatrain/backend-postgres
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash
    bun add @quatrain/core @quatrain/backend @quatrain/backend-postgres
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash
    # Annex OKF v0.1 knowledge base directly into your AI coding agent
    tycho knowledge install quatrain/quatrain-core
    ```
  </Tabs.Tab>
</Tabs>

### 2. Define a Domain Model

Define entity attributes, types, and validation rules declaratively:

```typescript
import { PersistedBaseObject } from '@quatrain/backend'
import { StringProperty, NumberProperty, BooleanProperty } from '@quatrain/core'

export class Product extends PersistedBaseObject {
  static COLLECTION = 'products'

  static PROPS_DEFINITION = [
    { name: 'name', type: StringProperty.TYPE, required: true },
    { name: 'sku', type: StringProperty.TYPE, required: true },
    { name: 'price', type: NumberProperty.TYPE, required: true },
    { name: 'inStock', type: BooleanProperty.TYPE, default: true }
  ]
}
```

### 3. Connect Adapter & Persist Data

Configure the backend adapter and perform operations through pre-bound repositories:

```typescript
import { Backend } from '@quatrain/backend'
import { PostgresAdapter } from '@quatrain/backend-postgres'
import { Product } from './models/Product'

async function bootstrap() {
  // 1. Initialize and register the backend adapter
  const postgres = new PostgresAdapter({
    config: {
      host: process.env.PG_HOST || 'localhost',
      port: 5432,
      database: 'quatrain_db',
      user: 'postgres',
      password: process.env.PG_PASSWORD || 'secret'
    }
  })
  Backend.addBackend(postgres, 'postgres', true)

  // 2. Instantiate and persist through dynamic repository
  const repo = Product.repository()
  const item = await Product.factory({
    name: 'prod_900',
    sku: 'SKU-QUATRAIN-01',
    price: 49.99,
    inStock: true
  })
  await repo.create(item)

  // 3. Read by UID
  const loaded = await repo.read('prod_900')
  console.log(`Product loaded: ${loaded?.val('sku')}`)
}

bootstrap()
```

---

## 📦 Ecosystem & Packages Map

The framework is organized into 7 logical architectural layers across 71 packages:

| Layer | Core Package | Ecosystem Adapters & Modules |
| :--- | :--- | :--- |
| **Domain & Entities** | [`@quatrain/core`](/packages/core/readme) | [`@quatrain/types`](/packages/types/readme) · [`@quatrain/state-machine`](/packages/state-machine/readme) · [`@quatrain/okf`](/packages/okf/readme) |
| **Databases & Persistence** | [`@quatrain/backend`](/packages/backend/readme) | [`backend-postgres`](/packages/backend-postgres/readme) · [`backend-sqlite`](/packages/backend-sqlite/readme) · [`backend-firestore`](/packages/backend-firestore/readme) · [`backend-restapi`](/packages/backend-restapi/readme) · [`backend-migrations`](/packages/backend-migrations/readme) |
| **Authentication & RBAC** | [`@quatrain/auth`](/packages/auth/readme) | [`auth-rbac`](/packages/auth-rbac/readme) · [`auth-supabase`](/packages/auth-supabase/readme) · [`auth-firebase`](/packages/auth-firebase/readme) · [`auth-oidc`](/packages/auth-oidc/readme) · [`auth-pocketbase`](/packages/auth-pocketbase/readme) · [`auth-http-basic`](/packages/auth-http-basic/readme) |
| **Object Storage** | [`@quatrain/storage`](/packages/storage/readme) | [`storage-s3`](/packages/storage-s3/readme) · [`storage-local`](/packages/storage-local/readme) · [`storage-supabase`](/packages/storage-supabase/readme) · [`storage-firebase`](/packages/storage-firebase/readme) · [`storage-git`](/packages/storage-git/readme) |
| **Queues & Messaging** | [`@quatrain/queue`](/packages/queue/readme) | [`queue-amqp`](/packages/queue-amqp/readme) · [`queue-mqtt`](/packages/queue-mqtt/readme) · [`queue-sqlite`](/packages/queue-sqlite/readme) · [`queue-aws`](/packages/queue-aws/readme) · [`queue-gcp`](/packages/queue-gcp/readme) · [`messaging`](/packages/messaging/readme) |
| **APIs & Gateways** | [`@quatrain/api`](/packages/api/readme) | [`api-client`](/packages/api-client/readme) · [`api-server`](/packages/api-server/readme) · [`api-server-express`](/packages/api-server-express/readme) · [`api-server-astro`](/packages/api-server-astro/readme) · [`api-xmlrpc`](/packages/api-xmlrpc/readme) |
| **AI, Chat & Cognitive** | [`@quatrain/ai`](/packages/ai/readme) | [`ai-gemini`](/packages/ai-gemini/readme) · [`chat`](/packages/chat/readme) · [`skills`](/packages/skills/readme) |
| **Platform & Tooling** | [`@quatrain/cli`](/packages/cli/readme) | [`cache`](/packages/cache/readme) · [`cache-redis`](/packages/cache-redis/readme) · [`log`](/packages/log/readme) · [`i18n`](/packages/i18n/readme) · [`mdm`](/packages/mdm/readme) · [`testing`](/packages/testing/readme) |

👉 **[Explore Full Interactive Packages Catalog](/packages)**

---

## 🧠 Open Knowledge Base (OKF v0.1) & Tycho Integration

Quatrain Core embeds an official knowledge base formatted according to the **Open Knowledge Format (OKF v0.1)** under [`okf/`](/okf).

Designed specifically for **AI pair-programming agents** (Gemini, Claude, Antigravity, Cursor) and human engineers, it provides authoritative, hallucination-free guidance across core architectural areas:
* 🏛️ **Domain Modeling**: [okf/domain-modeling](/okf/domain-modeling) (Schemas, `.TYPE`, FSM workflows)
* 💾 **Persistence**: [okf/persistence-adapters](/okf/persistence-adapters) (Repositories, PostgreSQL, SQLite, Migrations)
* 🔐 **Auth & Security**: [okf/auth-and-security](/okf/auth-and-security) (SSO, Supabase, RBAC, Field-Level Security)
* 🗄️ **Object Storage**: [okf/storage-and-assets](/okf/storage-and-assets) (Unified S3/MinIO & Local FS storage)
* 📬 **Queues & Streaming**: [okf/queues-and-events](/okf/queues-and-events) (RabbitMQ/AMQP, background workers)
* 🌐 **API Gateways**: [okf/api-and-gateways](/okf/api-and-gateways) (Isomorphic fetch client, CrudEndpoint)
* 🤖 **AI Agents**: [okf/ai-and-agents](/okf/ai-and-agents) (Gemini models, schema-guaranteed JSON generation)

### Annexing via Tycho CLI (`tycho knowledge`)

```bash
# 1. Register the Quatrain Core repository in Tycho
tycho repo add quatrain Quatrain/Core

# 2. Install the knowledge package
tycho knowledge install quatrain/quatrain-core

# 3. Check active knowledge packages
tycho knowledge list
```

---

## 🧰 Companion Projects

* **[Quatrain CoreApps](https://github.com/Quatrain/CoreApps)**: Pre-packaged Docker / Podman containers, including:
  * `api-gateway`: High-performance reverse proxy and routing gateway.
  * `studio-image`: Web modeling studio for entity scaffolding.
* **[Quatrain CoreUX](https://github.com/Quatrain/CoreUX)**: Mantine-based UI design tokens, components, high-contrast mobile interfaces, and voice adapters.
